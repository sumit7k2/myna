# Topics
query GetTopics {
  topics {
    ...TopicItem
  }
}

mutation SaveUserTopics($topicIds: [ID!]!) {
  saveUserTopics(topicIds: $topicIds)
}

# shared fragments
# import from src/graphql/fragments.graphql
